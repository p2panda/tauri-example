{
  "name": "p2panda-tauri-example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "peer": "HTTP_PORT=$(shuf -i 1024-65535 -n 1) tauri dev --no-watch --config '{\"build\": {\"beforeDevCommand\": \"\"}}'"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.4.0",
    "shirokuma": "^0.1.3"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.4.0",
    "vite": "^4.4.4"
  }
}